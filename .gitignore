# Nix build output
/result
/result-*

# git-hooks.nix generated (symlink into /nix/store, regenerated per devShell)
.pre-commit-config.yaml

# Go build artifacts
/bin/
*.test
*.out

# direnv (nix-direnv cache)
.direnv/
